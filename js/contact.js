document.addEventListener("DOMContentLoaded",()=>{const form=document.getElementById("contact-form"),out=document.getElementById("contact-message");form?.addEventListener("submit",async e=>{e.preventDefault();out.textContent="Mengirim...";const d=Object.fromEntries(new FormData(form).entries());const {error}=await window.supabaseClient.from("messages").insert({name:d.name,email:d.email,subject:d.subject||"",message:d.message});if(error){console.error(error);out.textContent=error.message;return;}out.textContent="Pesan berhasil dikirim.";form.reset();});});
